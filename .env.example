# PostgreSQL database connection string
DATABASE_URL="postgresql://your_username:your_password@localhost:5432/school_db"

# Port on which the server runs (optional, defaults to 3000 if not set)
PORT=3000
